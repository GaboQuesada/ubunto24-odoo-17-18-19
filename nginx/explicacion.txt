# saber si nginx esta instalado

sudo systemctl status nginx

# Verificar la versión

nginx -v

# Buscar en los paquetes instalados

dpkg -l | grep nginx

#instalar NGINX

sudo apt update

apt list --upgradable

sudo apt install nginx -y

# creamos el archivo gqsoluciones.conf

en /etc/nginx/sites-available

cd sites-available

nano gqsoluciones.conf

borar en 

cd..

cd sites-enabled

rm default

cd.. 

cd sites-available

sudo ln -s /etc/nginx/sites-available/gqsoluciones.conf /etc/nginx/sites-enabled/

sudo nginx -t

sudo systemctl restart nginx

# instalr lets script 

sudo apt install certbot python3-certbot-nginx -y

sudo certbot --nginx -d gustos.sistemaodoo.com -d gustos.sistemaodoo.com









